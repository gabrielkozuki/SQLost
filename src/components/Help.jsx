import React, { useState } from 'react';
import './Help.scss';

function Help() {
    const [isHovered, setIsHovered] = useState(false);

    return (
        <div className="help-panel">
            
        </div>
    );
}

export default Help;